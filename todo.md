# TODO

- [ ] TICKET-004: Run credential-backed login smoke test via `scripts/login-smoke-test.js` (use `data/credentials.json`, confirm landing-page request count after login, verify SSE reaches `COMPLETE`).
- [ ] TICKET-005: Run full maintenance smoke test once the portal can accept a new request (submit a real ticket, validate TinyFish response plus landing-page count update).
